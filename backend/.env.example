PORT=4000
NODE_ENV=development

# Postgres (docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/airecruiter?schema=public

# Redis (docker-compose)
REDIS_URL=redis://localhost:6379

# Supabase Auth (recommended)
SUPABASE_URL=
SUPABASE_JWT_AUD=authenticated

# External AI services (Phase 2 wiring)
OPENAI_API_KEY=
PINECONE_API_KEY=
PINECONE_INDEX=
